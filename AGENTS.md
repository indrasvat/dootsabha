# AGENTS.md

**The agent conventions for this repository live in [`CLAUDE.md`](CLAUDE.md).
Read it first — it is the single source of truth.**

**NEVER edit this file. Edit `CLAUDE.md` instead.**

Nothing else belongs here. Rules are not duplicated in this file, because two
copies drift and an agent then follows whichever it happened to read.

Language- and path-scoped rules load from `.claude/rules/`.
